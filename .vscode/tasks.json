{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "func",
      "command": "host start",
      "problemMatcher": "$func-python-watch",
      "isBackground": true,
      "dependsOn": "build",
      "options": {
        "cwd": "${workspaceFolder}/src"
      }
    },
    {
      "label": "build",
      "type": "shell",
      "command": "script/build",
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
